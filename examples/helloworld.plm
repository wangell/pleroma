~sys►ds
~sys►io
~sys►amoeba

ε UserProgram {ioinst : @far io►Io, aman : @far amoeba►Amoeba}

	blahblah : ds►Hashmap

	δ create() -> void
		blahblah = ds►Hashmap()

	δ main(env: u8) -> u8

		let zz : str = "Really works!"

		ioinst ! print(zz)

		↵ 0
