~sys►ds
~sys►io
~sys►amoeba

ε UserProgram {ioinst : @far io►Io, aman : @far amoeba►Amoeba}

	blahblah : ds►Hashmap

	δ create() -> void
		blahblah = ds►Hashmap()

	δ zig() -> void
		ioinst ! print ("zig")

	δ main(env: u8) -> u8

		ioinst ! print("yuppi1")
		ioinst ! print("yuppi2")

		! zig()

		↵ 0
